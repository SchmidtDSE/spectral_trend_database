project: dse-regenag
dataset: BiomassTrends
defaults:
  how: LEFT
  select: '*'
  using: sample_id
queries:
  scym_raw:
    join:
    - table: SCYM_YIELD
    - table: LANDSAT_RAW_MASKED
      using: sample_id, year
    table: SAMPLE_POINTS
    where:
    - key: year
      table: SCYM_YIELD
